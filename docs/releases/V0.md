# DVEntityLinking V0 Release Record

日期：2026-05-26

状态：Closed。

## 关闭结论

V0 已完成需求、设计、实现、测试、真实 LLM 条件 smoke、demo/acceptance 准备、用户验收反馈处置和用户复验确认。用户确认“认可”，V0 accepted and closed。

## 主输出件

| 类型 | 文件 |
| --- | --- |
| IR | [../baselines/v0/IR.md](../baselines/v0/IR.md) |
| SR | [../baselines/v0/SR.md](../baselines/v0/SR.md) |

## 关闭范围

- L0 Mock 实体目录与实体词表。
- Query 实体识别、匹配、链接、归一化和消歧。
- 实体详情查询与 Top-K 相似/相关实体检索。
- 轻量 Web UI 和 JSON API。
- OpenAI-compatible LLM adapter、Mock LLM、真实 LLM 条件化 smoke。
- PyCharm 可直接启动的 Web demo 和验收 smoke 脚本。
- 使用说明和本地日志输出。

## 验收反馈

| Finding | 处置 |
| --- | --- |
| AF-001：缺少可以直接启动的 Python 文件和日志查看方式 | 已补齐 `scripts/run_web_demo.py`、`scripts/run_acceptance_smoke.py` 和 `outputs/logs/` 日志输出 |
| AF-002：缺少使用说明文档 | 已补齐 `docs/USAGE.md` |

## 验证摘要

| 命令 | 结果 |
| --- | --- |
| `python scripts\run_acceptance_smoke.py --mode offline_demo` | 通过，`ok=true` |
| `python -m pytest` | 通过，`38 passed` |
| `python -m compileall -q src scripts` | 通过 |
| `git diff --check` | 通过 |
| 真实 LLM smoke | 通过，仅记录脱敏摘要 |

## 不进入 V0 的事项

- 真实 DV 生产接口和生产 payload。
- 自动修复、派单、变更或写操作闭环。
- L1/L2 样例进入验收 baseline。
- 生产级向量库或权限治理。
- Web UI 视觉设计验收。

## 后续

V0 关闭后，LLM 模式切换、真实 Query 样例、实体结构审视和检索算法规划进入 V1。
